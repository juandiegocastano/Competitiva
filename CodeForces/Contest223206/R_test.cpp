#include <bits/stdc++.h>
#include <typeinfo>
using namespace std;

int main()
{

    int n; cin >> n;
    int vec [n];

    for (int i = 0; i < n; i++)
    {
        cin >> vec[n];
    }

    for (int i = 0; i < n; i++)
    {
        cout<<"i: "<<i<<" vec[i]: "<<vec[i]<<"\n";
    }


    return 0;
}