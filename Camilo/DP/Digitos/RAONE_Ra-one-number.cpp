#include <bits/stdc++.h>
using namespace std;
typedef long long ll;


string s;
ll dp[18][2][100][100];
long long from, to;

bool dptop(int idx, bool top, int even, int odd){
    if(idx ==  
    

}

void solve(){
    ll a, b;
    cin>>a>>b;
    s = to_string(a-1);
    memset(dp, -1, sizeof dp);

}


int main(){
    int t;
    while(t--){
        solve();
    }
    return 0;
}